# v106 open team creation

Run `migration_v106_open_team_creation.sql` in the Supabase SQL Editor.

Any active, approved and visible player can then create a team immediately.
The Level 2 restriction remains only on higher-risk point transfers and reward
wishes.

No Edge Function deployment or new secret is required.
