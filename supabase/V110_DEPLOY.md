# V110 deployment

Run `migration_v110_multiple_team_challenges.sql` once in the Supabase SQL Editor.

This removes the one-challenge-per-team/week constraint, adds challenge names,
and updates the challenge read/write RPCs used by the app.
