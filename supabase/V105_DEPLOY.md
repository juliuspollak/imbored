# v105 player team invitations

Run `migration_v105_team_invitations.sql` in the Supabase SQL Editor.

Any team member can then invite an eligible player. The recipient receives an
unread Teams/chat notification and must accept or decline before membership is
changed. Team blocks remain enforced.

No Edge Function deployment or new secret is required.
